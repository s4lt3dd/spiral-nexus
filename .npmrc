# Pin exact versions on install (no ^ or ~) to avoid surprise upgrades.
save-exact=true
# Enforce the Node engine range in package.json.
engine-strict=true
# Do not run arbitrary install/postinstall scripts from dependencies.
# A common supply-chain vector. Re-enable per-package only if a build needs it.
ignore-scripts=true
